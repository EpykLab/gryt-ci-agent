---
# Production inventory for Gryt CI Agent
# Replace with your actual server IPs/hostnames

all:
  children:
    gryt_agents:
      hosts:
        agent-01:
          ansible_host: 24.199.123.211
          # ansible_user: root  # Override if different
          # ansible_port: 22    # Override if different

        # Add more agents as needed
        # agent-02:
        #   ansible_host: 10.0.1.6
        # agent-03:
        #   ansible_host: 10.0.1.7
